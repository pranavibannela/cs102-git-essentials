git pull : to pull the commited changes
git push :to push the commited changes
git add file name :to add a file to the repository
git commit -m "":to commit the changes
git status :to know the status of the repo
visual studio code(VSC) can be used to code python,html etc,edit text files,csv files,markdown files.Visual Studio code is an editor.We can also use the VSC as git hub website.When we open git hub website in VSC we have an option called markdown all in one,if we install it we can observe the changes we made .We can create files with acertain structure and method.It helps us to organise our document using markdown software.We can also view repositories which we created on github in Visual studio  code.
